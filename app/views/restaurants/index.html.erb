<div class="container p-5">
  <h1 class="pb-2">Restaurants</h1>
  <ul>
    <% @restaurants.each do |r| %>
      <li class="pb-2"><%= link_to(r.name, restaurant_path(r), class: "link-dark") %></li>
    <% end %>
  </ul>
</div>
